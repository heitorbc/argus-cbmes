# Sprint S2.10.7a — Mapa Força: gate MF + VTRs status + filtra órfãs

**Data:** 2026-05-18
**Tech Lead:** 2º SGT Heitor Barcellos Coelho — NF 3037509
**PR:** TBD

## Motivação

O sprint S2.10.7 do roadmap previa 7 frentes pesadas no Mapa Força (5 dias estimados). Para manter PRs gerenciáveis com smoke test efetivo, dividi em **dois sub-PRs**:

- **S2.10.7a (este PR)** — Partes A, B, D, E: fixes do gate + comportamento de VTRs BAIXADA/EMPRESTADA + filtro de viaturas órfãs na UI + verificação de funcionalidade já existente (Parte E)
- **S2.10.7b (futuro)** — Partes C, F, G: aprovação individual de trocas autorizadas + escala especial empenho em recurso + aprovação individual de dispensas/atestados (envolvem schema novo de `statusAprovacao`)

## DoD (S2.10.7a)

- [x] **Parte A**: Botão "Preencher MF CIODES" exige `estado === 'VIATURA_CONFERIDA'` (não basta `EQUIPE_CONFERIDA`). Botão visível mas inativo enquanto há pendências, com lista textual ("Pendente: conferência do COV/Motorista").
- [x] **Parte B**: VTRs BAIXADA/EMPRESTADA não recebem efetivo do XLSX no recurso. Efetivo previsto fica em campo novo `ComposicaoMfEntry.efetivoPrevistoNaoPreenchido[]` e UI exibe banner amarelo na seção Tripulação listando quem ficou de fora.
- [x] **Parte D**: 5 viaturas órfãs (ABTS_011, AR_031, ATB_001, AU_154, TE_110) ocultadas do bloco "Viaturas (status do MF)" no frontend. Backend mantém os dados intactos (decisão D3 do plano).
- [x] **Parte E**: confirmação de que `AtivarRecursoCard` (apps/web/src/pages/mapa-forca-detalhe.tsx:2997+) já está funcional desde S6e — admin obrigatório do chefe, viaturas filtradas. Nenhuma mudança nesta entrega.
- [x] Typecheck + lint + format:check + build + 568 tests ✓
- [x] Schema migration: nenhuma (campo novo é só JSONB derivado em runtime)

## Mudanças por área

### Parte A — Gate do botão MF CIODES

`apps/web/src/pages/mapa-forca-detalhe.tsx:1508+`

**Antes:**

```ts
const podePreencherInicial = estado === 'VIATURA_CONFERIDA' || estado === 'EQUIPE_CONFERIDA';
```

**Depois:**

```ts
const podePreencherInicial = estado === 'VIATURA_CONFERIDA';
const aindaConferindo = estado === 'INICIADO' || estado === 'EQUIPE_CONFERIDA';
const pendencias: string[] = [
  // ...preenche conforme estado
];
```

UI: botão aparece mesmo em estados intermediários (INICIADO, EQUIPE_CONFERIDA) **com aparência cinza inativo + título "🔒 Preencher Mapa Força CIODES (bloqueado)"** e texto explicando o que falta. Quando ambas conferências terminam, botão fica verde ativo.

### Parte B — VTRs BAIXADA/EMPRESTADA não preenchem efetivo

**Schema** `packages/shared-types/src/mapa-forca.ts:75-100`:

```ts
export const composicaoMfEntrySchema = z.object({
  // ...campos existentes
  efetivoPrevistoNaoPreenchido: z
    .array(z.object({ funcao: z.string(), militar: composicaoMfMilitarSchema }))
    .default([]),
});
```

**Backend** `apps/api/src/modules/mapa-forca/mapa-forca.service.ts:660-680` em `buildComposicaoMf`:

- Quando `entry.vtrStatus === 'BAIXADA' || 'EMPRESTADA'`, em vez de preencher `chefe/motorista/operadores`, empilha o militar em `efetivoPrevistoNaoPreenchido` + marca `semEquipe: true`.

**UI** `mapa-forca-detalhe.tsx` (logo antes de "Realocações internas"):
Banner amarelo por recurso afetado:

> ⚠ RESGATE 02 (AR_044) está **BAIXADA** — efetivo escalado NÃO foi preenchido:
>
> - **Ch**: 2ºSGT BARCELLOS
> - **Mot**: CB VICENTE
>
> Após o início do serviço, confirme individualmente cada militar...

(O fluxo de "confirmação individual" pós-início do serviço fica para S2.10.7b — aqui apenas a sinalização visual está pronta.)

### Parte D — Filtra 5 viaturas órfãs

`apps/web/src/pages/mapa-forca-detalhe.tsx:35-50`:

```ts
const VIATURAS_ORFAS_OCULTAS = new Set(['ABTS_011', 'AR_031', 'ATB_001', 'AU_154', 'TE_110']);

function isViaturaOrfaOculta(codigoOuPrefixo: string | null | undefined): boolean {
  if (!codigoOuPrefixo) return false;
  const normalized = codigoOuPrefixo.replace(/\s+/g, '_').toUpperCase();
  return VIATURAS_ORFAS_OCULTAS.has(normalized);
}
```

Aplicado no bloco "Viaturas (status do MF)" — viatura órfã não aparece como card standalone, mas continua aparecendo dentro de um recurso se ativada via "Ativar recurso adicional" (backend inalterado).

### Parte E — Ativar recurso adicional

Verificado durante investigação: `AtivarRecursoCard` (linha 2997+) já cobre o requisito do plano (chefe obrigatório, viaturas filtradas, validação anti-duplicação). Sem mudança nesta entrega.

## Verificação

```bash
pnpm typecheck      # ✓
pnpm lint           # ✓
pnpm format:check   # ✓
pnpm test           # ✓ 568/568
pnpm build          # ✓
```

Schema mudou apenas tipos shared-types (`ComposicaoMfEntry.efetivoPrevistoNaoPreenchido`). Sem migration Prisma necessária — campo nasce em runtime no `buildComposicaoMf`.

## Smoke test pós-deploy

1. `/mapa-forca/<data>` em estado INICIADO:
   - Bloco "Conferências" mostra checkboxes de equipe + viatura + IDEO
   - Box do botão MF aparece **cinza** com "🔒 Preencher Mapa Força CIODES (bloqueado)" + "Pendente: conferência do COV/Motorista (viatura)"
2. Completar equipes → estado vira EQUIPE_CONFERIDA:
   - Box ainda cinza com pendência atualizada
3. Completar viaturas → estado VIATURA_CONFERIDA:
   - Box fica **verde** com botão ativo
4. Cenário VTR BAIXADA:
   - Marcar `AR_044` como BAIXADA no admin
   - Importar escala com militares escalados nesse recurso
   - Prévia: banner amarelo aparece listando efetivo que ficou de fora
5. Confirmar que as 5 viaturas órfãs não aparecem mais standalone em "Viaturas (status do MF)"

## Próximo (S2.10.7b)

Partes C/F/G juntas — todas dependem de schema novo:

- **Parte C**: `previaTrocaSchema` ganha `statusAprovacao: 'pendente'|'aprovada'|'rejeitada'`; endpoint `PATCH /previa/:data/trocas/:id/aprovar`
- **Parte F**: `EscalaEspecialAto` ganha `empenhoEmRecurso: { recurso, periodoInicio, periodoFim, substituidoNf?, destinoSubstituido? }`
- **Parte G**: `PreviaDispensa`/`PreviaAtestado` ganham `statusAprovacao`; endpoints `/dispensas/:id/aprovar` + `/atestados/:id/aprovar`

Estimativa: ~2 dias para S2.10.7b.
