{
  "name": "argus-cbmes",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de Gestão Operacional do 1º BBM — 1ª Cia/1º BBM CBMES",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api start:dev",
    "build:types": "pnpm --filter @argus/shared-types build",
    "build": "pnpm build:types && pnpm --filter web build && pnpm --filter api build",
    "lint": "pnpm -r lint",
    "test": "pnpm build:types && pnpm -r test",
    "typecheck": "pnpm build:types && pnpm -r typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "scrape:ideo": "node scripts/scrape-ideo-cbmes.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "globals": "^17.6.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.59.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "puppeteer"
    ]
  }
}
